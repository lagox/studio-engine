<%= form_for(@ourproject) do |f| %>
  <% if @ourproject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ourproject.errors.count, "error") %> prohibited this ourproject from being saved:</h2>

      <ul>
      <% @ourproject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
